import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dadimaa.express',
  appName: 'Dadi Maa Express',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;